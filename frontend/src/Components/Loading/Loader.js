

const Loader = () => {
    return (
        <div className="bg-black h-screen w-screen flex items-center justify-center">
            <img src="/logo192.png" alt="" className="animate-spin h-14" />
        </div>
    );
};

export default Loader;
